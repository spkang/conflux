[package]
name = "conflux"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
anyhow.workspace = true
conflux-core = { path = "../crates/core" }
conflux-files = { path = "../crates/files" }
conflux-terminal = { path = "../crates/terminal" }
conflux-webpanel = { path = "../crates/webpanel" }
serde.workspace = true
serde_json.workspace = true
tauri = { version = "2", features = [] }
thiserror.workspace = true

